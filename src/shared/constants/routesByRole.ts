export const initialRoutesByRole: Record<number, string> = {
    1: "/",               // Admin
    2: "/projects",       // Supervisor
    3: "/readonly",       // Observador
    4: "/contractor",     // Contratista
};